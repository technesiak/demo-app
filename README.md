# Notes service

### Run project

build:
```commandline
docker compose build
```

start:
```commandline
docker compose up
```

stop:
```commandline
docker compose down
```

run-background:
```commandline
docker compose up --detach
```
